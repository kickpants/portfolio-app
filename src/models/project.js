class Project {
  constructor(id, title, summary, description, thumbnail, link) {
    this.id = id;
    this.title = title;
    this.summary = summary;
    this.description = description;
    this.thumbnail = thumbnail;
    this.link = link;
  }
}

export default Project;
